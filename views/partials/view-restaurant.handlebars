<section class="sidebar">
    <h2>More Options</h2>
    <button class="sidebar-btn" id="add-restaurant">Edit This Restaurant</button>
    <button class="sidebar-btn" id="add-restaurant">Change My Password</button>
    <button class="sidebar-btn" id="add-restaurant">Log Out</button>
    {{!-- will need to add script to to make Edit Restaurant button link to edit page (or use link instead of button)
    --}}
    <script src="/js/update-user.js"></script>
    {{!-- no need to add script for logout.js here, since it's conditionally added on main.handlebars --}}
</section>
<main>
    {{> restaurant-info restaurant}}
    <h2>
        {{restaurant.name}}
    </h2>
    <div class="restaurant-view">
        <p><a href="/">&lt;&lt;Back to My To-Dine List</a></p>
        <img src="/img/{{restaurant.imgName}}.jpg" class="restaurant-view-img">
        {{!-- will need to add imgName key to each imported restaurant object with filename of associated image --}}
        <p>Cuisine: <span class="red-text">{{restaurant.cuisine}}</span></p>
        <p>Location: <span class="red-text">{{restaurant.location}}</span></p>
        <p>Rating: <span class="red-text">{{restaurant.rating}}</span></p>
        {{!-- will need to update code above to show rating stars or No Rating text --}}
        <p>
            Notes:<br />
            <span class="red-text">{{restaurant.notes}}</span>
        </p>
    </div>
</main>